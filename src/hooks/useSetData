import { useEffect, useState } from "react";

function useSetData(data) {
    const [value, setValue] = useState(null)
    
    useEffect(() => {
            setValue(data)
    }, [data])

    return value
}

export default useSetData